# Hack 模板

每条 Hack 是一个独立 Markdown 文件，放在 `hacks/` 目录下，文件名即 `id`。

**规则：26 个字段全部必须存在。** 短字段放 YAML Front Matter，长字段作为正文章节。如果某个字段确实没有内容，写 `none` 并说明原因，不能删除字段。

---

## YAML Front Matter（元数据）

```yaml
---
id: your-hack-id                 # 唯一标识，与文件名一致，kebab-case
title: 一句话标题（含目标、人群、时间范围）  # 例：本科生 6-12 个月进入 AI 科研闭环的高胜率路径
domain: 领域标签                  # 例：academia/research-ai, learning/engineering, career/visibility
version: 0.1.0                    # semver：新证据小改 0.x，重大路径重写升主版本
status: draft                     # draft | active | contested | archived
last_updated: 2026-08-17          # ISO 日期，改动必须更新

# ---- 目标与对象 ----
target_outcome: |
  可观察、可验证的结果。禁止"变优秀""提高认知"这类不可验证目标。
  例："在 6-12 个月内产出可复现的 AI 研究仓库 + 论文投稿，并获得目标课题组的合作或申请机会。"
target_user: |
  这条路径为谁而写。具体到：阶段、现有能力、可投入时间、资源、目标。
required_starting_conditions:
  - 硬性前置条件。不满足则路径不适用或必须先补。
  - 例："能独立读写 Python"；"每周可投入 ≥10 小时"
context_variables:
  # 决定分支的变量，AI 判定用户属于哪个分支时逐个询问
  - 变量名: 取值说明（例：编程水平: beginner | intermediate | advanced）

# ---- 时间与约束 ----
time_horizon: 6-12 个月
constraints:
  - 红线。例："不允许学术造假/代写/虚假包装"；"不泄露未发表研究"
disqualifying_conditions:
  - 出现任一条件即判定此路径不适用，必须转向其他 Hack 或明说"无适用路径"。

# ---- 证据 ----
evidence_strength: C   # A: 多来源数据/高质量研究; B: 大量可复现案例; C: 有限案例/经验/强逻辑; D: 待验证
---
```

## 正文章节

### 1. recommended_path

主推荐路径的总述：**3-5 句话讲清这条路径的核心逻辑**（先做什么、关键杠杆是什么、为什么顺序如此）。禁止并列展示多条等价路径；如果存在分支，先写明"分叉变量"，再给主分支的推荐，次要分支简要说明差异即可。

### 2. immediate_next_action

用户读完这条 Hack 后，**今天或一小时内**能做的具体动作。必须是可执行的动词短语："clone 这个仓库并跑通"、"写 200 字给学长发消息"、"注册并发布第一篇帖子"。禁止"制定计划""开始学习"这类不可执行表述。

### 3. step_by_step_actions

分阶段行动序列，每阶段带时间范围。格式：

```
阶段 1（第 1-2 周）：阶段目标
1. 具体动作
2. 具体动作
   - 产出物：xxx（必须列出）
阶段 2（第 3-6 周）：阶段目标
...
```

每阶段必须列出**产出物**。没有产出物的阶段设计是有缺陷的。

### 4. checkpoints

| 时间点 | 检查内容 | 通过标准 | 未通过的处理 |
|---|---|---|---|
| 1 周 | 例：第一条真实产出是否出现 | 例：仓库跑通/发出第一封有效接触 | 例：检查前置条件是否误判 |

### 5. success_metrics

量化或可判定的指标。例："3 个月内完成 1 个端到端复现项目"、"6 个月内导师明确同意带你做课题"。指标必须对应 `target_outcome`。

### 6. failure_signals

什么迹象说明：路径不适用 / 执行方式错误 / 应该转向。写**可观察的信号**，例："连续 2 周没有任何进展且没有外部反馈"、"导师回复率降为 0"。

### 7. pivot_rules

转向规则：触发条件 → 转向动作 → 转向后的新分支。例："若第 6 周仍无课题切入点 → 放弃该导师方向，转向自选可执行课题，路径不变但更换入口"。

### 8. disqualifying_conditions

（YAML 中已声明，此处展开说明每条为什么是硬门槛。）

### 9. common_traps

这条路径最常见的失败方式，每条一行：陷阱 + 为什么 + 怎么避免。

### 10. why_this_works

这条路径成立的机制解释（系统奖励什么、瓶颈在哪、杠杆在哪）。**逻辑论证，不是口号。**

### 11. evidence

分条列出，每条带来源类型和等级标注：

```
- [B] 大量可复现案例：……（描述）
- [C] 维护者经验：……
- [D] 待验证假设：……
```

### 12. known_counterexamples

已知反例：这条路径在什么情况下失败了 / 被谁反驳了 / 存在什么证据冲突。没有反例说明维护者还没认真找过，标记为"尚未发现，待收集"。

### 13. risks

风险与副作用，包括：失败概率、成本上限、名誉/安全/法律风险、对长期能力建设的可能损害。**风险必须诚实，禁止为了说服而弱化。**

### 14. maintainer_notes

维护者备注：哪些内容是从原始素材校准/抽象过的（例："小红书"被抽象为"目标机会实际存在的平台"）、哪些声明尚未验证、下一个验证动作是什么。

---

## 写作禁忌（违反即打回）

- ❌ "因人而异""适合自己的才是最好的""没有标准答案"——出现即无效
- ❌ 用"努力、坚持、打基础、建立人脉"充当行动步骤
- ❌ 把单一成功者的事后叙事写成规律
- ❌ 无条件的"最优"表述（必须绑定条件变量）
- ❌ 观点与事实混在一起不标证据等级
- ❌ 为了显得全面而堆砌低价值条目
- ❌ 把短期拿结果和长期能力建设混为一谈
